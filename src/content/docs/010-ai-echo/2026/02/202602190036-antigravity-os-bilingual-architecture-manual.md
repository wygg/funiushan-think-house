---
created: '2026-02-19 00:36:43'
date: 2026-02-19
publish:
- Garden
tags:
- Project/Antigravity
- System/Architecture
- i18n
title: Antigravity OS 中英双语架构全景手册 (Bilingual Architecture Manual)
ts: 1771432603
updated: '2026-02-19 23:17:02'
---

# 🌌 Antigravity OS 中英双语架构全景手册 (Bilingual Architecture Manual)

> **版本**：2.0 (i18n Edition)  
> **日期**：2026-02-19  
> **核心定义**：一个基于“知行合一”理念，由 AI Agent 托管，支持中英双语自动化发布的个人数字生命系统。

---

## 1. 物理架构 (Physical Architecture)

系统采用“三层漏斗”模型，实现从私密创作到全球发布的自动分层。

```text
myAntigravity/ (本地总库 - Private)
├── .cursorrules           # 📜 AI 宪法：规定了 AI 必须执行的翻译与发布准则
├── Library_Study-Think/   # 🧠 大脑：Obsidian 知识库 (私有)
│   ├── 000_Daily_Note/    # 原始日记：允许使用中文名，记录碎片思考
│   └── ...
├── scripts/               # ⚙️ 工厂：Python 自动化脚本 (如 sync_garden.py)
└── 800_Digital_Garden/    # 🌍 展示：Astro Starlight 项目 (公开 Repo)
    ├── src/content/docs/
    │   ├── (root)         # 🇨🇳 中文主站 (如 garden/, blog/)
    │   └── en/            # 🇺🇸 英文站点 (文件夹隔离)
    └── astro.config.mjs   # 站点核心配置 (i18n 逻辑)
```

---

## 2. 国际化 (i18n) 核心实现

为了让全球用户都能读懂你的思考，我们建立了完善的翻译映射。

### 2.1 路由规则
*   **中文 (Root)**: `digital-garden.vercel.app/`
*   **英文 (Locale)**: `digital-garden.vercel.app/en/`

### 2.2 站点配置 (Starlight)
在 `astro.config.mjs` 中定义了：
*   **多语言支持**: 根路径指向简体中文，`/en/` 路径指向英文。
*   **侧边栏翻译**: 使用 `translations` 字段，确保界面标签（如“数字花园” $\leftrightarrow$ "Digital Garden"）随语言自动切换。

---

## 3. 自动化发布流水线 (The Pipeline)

这是 Antigravity OS 的核心灵魂，将繁琐的“翻译+导图+部署”转化为一键指令。

### 3.1 发布标准作业程序 (SOP)
1.  **创作 (Write)**: 在 Obsidian 中书写中文，在末尾打上 `#public`。
2.  **指令 (Command)**: 向 AI 发出“发布花园”指令。
3.  **同步 (Sync)**: AI 运行脚本，将符合条件的文件搬运至 `800_Digital_Garden`。
4.  **翻译 (Translate)**: **[Agent 托管]** AI 自动读取新增中文内容，生成地道的英文版存入 `en/` 目录。
5.  **部署 (Deploy)**: AI 自动执行 Git Push，触发 Vercel 云端构建及全球 CDN 分发。

---

## 4. 后勤保障 (Infrastructure)

*   **图片处理**: 自动接入 **Cloudflare R2**。无论本地文件名多乱，上传时自动 Sanitization（清洗）为英文 Slug 链接。
*   **搜索**: 集成 **Pagefind**。支持中英双语全文检索 (Cmd+K)。
*   **安全性**: 严守 **`.env`** 墙。所有密钥不入库，数据主权归本人所有。

---

## 5. AI Agent 协议 (The Constitution)

在 `.cursorrules` 中，显式规定了以下“钢铁律法”：
- **翻译义务**: 只要是发布，Agent 必须同步更新英文版。
- **降噪义务**: 严禁将中文名带入输出目录，必须转换为英文 Slug。
- **分层义务**: 严禁在脚本中引入对私有库 `999_Private_Vault` 的扫描。

---

## 🌟 结语 (Summary)

**Antigravity OS** 不仅仅是一个博客，它是你思考的“外骨骼”。你负责最感性的、大白话的中文思考，系统（AI）负责将其净化、翻译并以最顶尖的 Web 技术呈献给世界。

> **"Leave the dirty work to the AI, leave the deep thoughts to the human."**

---
#public